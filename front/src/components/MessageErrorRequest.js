const MessageErrorRequest = ({ children }) => (
    <div style={{
        fontFamily: "'Inter', sans-serif",
        boxSizing: "border-box",
        borderRadius: ".25rem",
        border: "1px solid transparent",
        marginBottom: "1rem",
        padding: "1rem 1rem",
        borderColor: "#f5c2c7",
        backgroundColor: "#f8d7da",
        color: "#842029"
    }}>
        {children}
    </div>
)


export default MessageErrorRequest
